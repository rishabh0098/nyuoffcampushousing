-- Historical placeholder. Stern was originally drafted here, then applied in
-- 20260710010000_add_furnished_bath_ac_neighborhood_enum via:
--   ALTER TYPE "Campus" ADD VALUE IF NOT EXISTS 'Stern';
-- Keep this migration name so deploy history stays consistent across databases.
SELECT 1;
